# 🚀 Развёртывание на Render.com

## ⚠️ Важно: Режимы работы

Render имеет **два режима** работы для этого проекта:

| Режим | Тариф | WebSocket | Производительность |
|-------|-------|-----------|-------------------|
| **HTTP Long-Polling** | Free | ❌ | ⭐⭐⭐ |
| **WebSocket** | Starter ($7/мес) | ✅ | ⭐⭐⭐⭐⭐ |

---

## 🆓 Вариант 1: Render Free (HTTP Long-Polling)

Бесплатный тариф не поддерживает WebSocket, поэтому используется HTTP long-polling.

### Настройка

1. **Создайте новый Web Service** на https://render.com
2. **Подключите GitHub репозиторий** `Pudge_Wars`
3. **Настройки:**
   - **Name**: `pudge-wars`
   - **Region**: Oregon (или ближе к вам)
   - **Branch**: `main`
   - **Root Directory**: (оставьте пустым)
   - **Runtime**: `Node`
   - **Build Command**: `npm install`
   - **Start Command**: `node server/http-server.js`
   - **Plan**: **Free**

4. **Advanced → Environment Variables:**
   ```
   NODE_VERSION=22.22.0
   PORT=3000
   HOST=0.0.0.0
   ```

5. **Create Web Service**

### После развёртывания

Вы получите URL вида:
```
https://pudge-wars-<random>.onrender.com
```

**Откройте клиент:**
```
https://pudge-wars-<random>.onrender.com/client/
```

### ⚠️ Ограничения Free тарифа

- Сервер "засыпает" через 15 минут бездействия
- Первое подключение занимает ~30 секунд (cold start)
- HTTP long-polling вместо WebSocket (меньшая производительность)
- 750 часов в месяц бесплатно

---

## 💎 Вариант 2: Render Starter ($7/мес)

Платный тариф поддерживает полноценный WebSocket.

### Настройка

1. **Создайте новый Web Service**
2. **Те же настройки, но:**
   - **Start Command**: `npm start`
   - **Plan**: **Starter** ($7/мес)

3. **Environment Variables:**
   ```
   NODE_VERSION=22.22.0
   PORT=3000
   HOST=0.0.0.0
   ```

### Преимущества

- ✅ Полноценный WebSocket
- ✅ Нет cold start
- ✅ Выше производительность
- ✅ 750 часов в месяц

---

## 📋 Пошаговая инструкция

### Шаг 1: Подготовка репозитория

```bash
# Убедитесь, что все файлы закоммичены
git add .
git commit -m "Ready for Render deployment"
git push origin main
```

### Шаг 2: Создание сервиса на Render

1. Перейдите на https://dashboard.render.com
2. Нажмите **New +** → **Web Service**
3. **Connect repository**: Выберите ваш GitHub репозиторий

### Шаг 3: Конфигурация

Заполните форму:

```
Name: pudge-wars
Region: Oregon (us-west-1)
Branch: main
Root Directory: (пусто)
Runtime: Node
Build Command: npm install
Start Command: node server/http-server.js  (для Free)
               npm start                   (для Starter)
Plan: Free или Starter
```

### Шаг 4: Переменные окружения

Раскройте **Advanced** → **Add Environment Variable**:

```
KEY=NODE_VERSION    VALUE=22.22.0
KEY=PORT            VALUE=3000
KEY=HOST            VALUE=0.0.0.0
```

### Шаг 5: Деплой

Нажмите **Create Web Service**

Развёртывание займёт 2-5 минут.

### Шаг 6: Проверка

1. Откройте URL из dashboard
2. Добавьте `/client/` к URL
3. Подключитесь к игре

---

## 🔧 render.yaml (Автоматическое развёртывание)

Файл `render.yaml` уже настроен для Free тарифа:

```yaml
services:
  - type: web
    name: pudge-wars
    env: node
    region: oregon
    plan: free
    buildCommand: npm install
    startCommand: node server/http-server.js
    envVars:
      - key: NODE_VERSION
        value: 22.22.0
      - key: PORT
        value: 3000
      - key: HOST
        value: 0.0.0.0
```

### Деплой через Render CLI

```bash
# Установка CLI
npm install -g @render-cloud/cli

# Авторизация
render login

# Деплой
render up
```

---

## 🐛 Troubleshooting

### "Upgrade Required"

WebSocket требует платный тариф. Используйте HTTP режим:
```
Start Command: node server/http-server.js
```

### Сервер "засыпает"

Free тариф засыпает через 15 минут бездействия. Решения:
1. Используйте платный Starter ($7/мес)
2. Отправляйте ping каждые 10 минут (uptime robot)
3. Используйте альтернативы (Railway, Fly.io)

### Холодный старт (долгая загрузка)

Первое подключение после простоя занимает ~30 секунд. Это нормально для Free тарифа.

### Клиент не подключается

1. Проверьте URL (должен быть `https://`)
2. Откройте консоль браузера (F12) для ошибок
3. Убедитесь, что сервер развёрнут успешно

### Ошибка сборки

Проверьте логи в dashboard Render:
```
Logs → View Logs
```

Частые проблемы:
- `npm install` не найден → Убедитесь, что `package.json` существует
- `Cannot find module` → Проверьте пути импортов

---

## 📊 Мониторинг

### Логи

В dashboard Render:
```
Logs → View Logs
```

### Метрики

```
Metrics → View Metrics
```

Показывает:
- CPU usage
- Memory usage
- Request count
- Response time

---

## 💡 Советы

### Оптимизация для Free тарифа

1. **Уменьшите TICK_RATE** в `server/config.js`:
   ```javascript
   TICK_RATE: 30,  // вместо 60
   ```

2. **Увеличьте интервал polling** в клиенте:
   ```javascript
   this.pollingInterval = 200;  // вместо 100
   ```

3. **Включите gzip сжатие** (добавьте в http-server.js):
   ```javascript
   const compression = require('compression');
   app.use(compression());
   ```

### Переход на платный тариф

1. В dashboard Render выберите сервис
2. Settings → Plan → Starter
3. Измените Start Command на `npm start`
4. Deploy → Manual Deploy → Deploy

---

## 🎮 Тестирование

1. Откройте 2+ вкладок браузера
2. Подключитесь с разными именами
3. Создайте комнату в одной вкладке
4. Присоединитесь в другой
5. Нажмите "Start Game"
6. Играйте! (WASD + мышь)

---

## 📞 Поддержка

- **Документация Render**: https://render.com/docs
- **Статус сервиса**: https://status.render.com
- **Support**: https://render.com/support

---

## 🔄 Обновление после развёртывания

```bash
# Внесите изменения в код
git add .
git commit -m "Update game logic"
git push origin main

# Render автоматически перезадеплоит
```

Или вручную в dashboard:
```
Manual Deploy → Deploy latest commit
```
