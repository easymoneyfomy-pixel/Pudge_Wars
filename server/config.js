/**
 * Конфигурация сервера
 */
module.exports = {
  // Сетевые настройки
  PORT: process.env.PORT || 3000,
  HOST: '0.0.0.0',
  
  // Настройки игры
  GAME: {
    TICK_RATE: 60,                    // Обновлений в секунду
    ARENA_WIDTH: 1200,                // Ширина арены
    ARENA_HEIGHT: 800,                // Высота арены
    PLAYER_RADIUS: 20,                // Радиус игрока
    PLAYER_SPEED: 200,                // Скорость игрока (пикселей/сек)
    PLAYER_MAX_HP: 100,               // Максимальное здоровье
    RESPAWN_TIME: 3000,               // Время возрождения (мс)
  },
  
  // Настройки хука
  HOOK: {
    SPEED: 600,                       // Скорость полёта хука
    RANGE: 500,                       // Максимальная дальность
    RADIUS: 8,                        // Радиус хука
    COOLDOWN: 2000,                   // Перезарядка (мс)
    PULL_SPEED: 300,                  // Скорость притягивания
    DAMAGE: 25,                       // Урон от хука
  },
  
  // Настройки комнат
  ROOM: {
    MAX_PLAYERS: 10,                  // Максимум игроков в комнате
    MIN_PLAYERS: 2,                   // Минимум для старта
    AUTO_START_DELAY: 5000,           // Задержка перед авто-стартом
  },
  
  // Анти-чит
  ANTI_CHEAT: {
    MAX_SPEED_MULTIPLIER: 1.5,        // Максимальный множитель скорости
    POSITION_CHECK_INTERVAL: 100,     // Интервал проверки позиции (мс)
    MAX_POSITION_DIFF: 50,            // Максимальная разница позиций
  },
  
  // Синхронизация
  SYNC: {
    STATE_BROADCAST_INTERVAL: 33,     // Интервал отправки состояния (33мс ~ 30 FPS)
    INPUT_BUFFER_SIZE: 10,            // Размер буфера ввода
  },
};
