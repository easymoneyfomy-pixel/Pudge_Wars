# ✅ Render FREE - Рабочее решение 2026

## 🔧 Проблема "Upgrade Required"

Render Free **блокирует все WebSocket соединения** на бесплатном тарифе.

## ✅ Решение

**Pure HTTP Server** - полностью независимая версия без WebSocket зависимостей.

---

## 🚀 Развёртывание за 3 шага

### 1️⃣ Запушьте изменения

```bash
git add .
git commit -m "Add pure HTTP server for Render Free"
git push origin main
```

### 2️⃣ Создайте Web Service на Render

1. Перейдите на https://dashboard.render.com
2. **New +** → **Web Service**
3. Подключите ваш GitHub репозиторий

### 3️⃣ Настройте

```
Name: pudge-wars
Region: Oregon (или ближе к вам)
Branch: main
Runtime: Node
Build Command: npm install
Start Command: node server/http-pure.js
Plan: Free
```

**Environment Variables:**
```
NODE_VERSION=22.22.0
PORT=3000
```

**Нажмите "Create Web Service"**

---

## 🎮 Открыть игру

После деплоя (2-5 минут):

```
https://pudge-wars-<random>.onrender.com/
```

Или напрямую клиент:
```
https://pudge-wars-<random>.onrender.com/client/index-http.html
```

---

## 📁 Новые файлы

| Файл | Назначение |
|------|------------|
| `server/http-pure.js` | ✅ Pure HTTP сервер (без WebSocket) |
| `client/index-http.html` | ✅ HTTP версия клиента |
| `client/js/NetworkHTTP.js` | ✅ HTTP long-polling модуль |
| `render.yaml` | ✅ Обновлён для http-pure.js |

---

## 🔍 Как это работает

### HTTP Long Polling

```
Клиент                          Сервер
  │                               │
  │─── POST /api/register ───────▶│
  │◀── { playerId } ──────────────│
  │                               │
  │─── GET /api/poll ────────────▶│
  │                               │ (ждёт 25 сек)
  │◀── [messages] ────────────────│ (если есть)
  │                               │
  │─── GET /api/poll ────────────▶│ (сразу новый запрос)
  │                               │
```

### Преимущества

- ✅ **Работает на Render Free**
- ✅ **Нет WebSocket - нет блокировки**
- ✅ **Карта не требуется**
- ✅ **Все функции игры**

### Недостатки

- ⚠️ Задержка 100-500ms (вместо 10-50ms)
- ⚠️ Сервер "засыпает" через 15 мин
- ⚠️ Холодный старт ~30 сек

---

## 🧪 Локальное тестирование

### Запуск

```bash
node server/http-pure.js
```

### Откройте браузер

```
http://localhost:3000/
```

Или напрямую:
```
http://localhost:3000/client/index-http.html
```

---

## 🔄 Сравнение версий

| Версия | Файл | Зависимости | Render Free |
|--------|------|-------------|-------------|
| **WebSocket** | `server/src/index.js` | ws | ❌ Требуется Starter ($7) |
| **HTTP v1** | `server/http-server.js` | Менеджеры | ⚠️ Может блокироваться |
| **HTTP Pure** | `server/http-pure.js` | Нет | ✅ Работает |

---

## 🐛 Troubleshooting

### "Upgrade Required"

Убедитесь, что используете:
- ✅ Start Command: `node server/http-pure.js`
- ✅ Клиент: `index-http.html`
- ✅ Сеть: `NetworkHTTP.js`

### 404 ошибка

Подождите окончания деплоя в dashboard Render.

### Таймаут / Cold start

Сервер "засыпает" через 15 мин бездействия. Откройте URL заново.

### Игра не подключается

1. Откройте консоль браузера (F12)
2. Проверьте ошибки в Console и Network
3. Убедитесь, что сервер развёрнут успешно

### "Cannot find module"

Убедитесь, что `npm install` выполнился:
```
Logs → View Logs
```

---

## 📊 Производительность

| Параметр | HTTP Pure | WebSocket |
|----------|-----------|-----------|
| Задержка | 100-500ms | 10-50ms |
| Запросов/сек | 10-20 | 1 (постоянно) |
| Трафик | Больше | Меньше |
| Цена | $0 | $7/мес |

---

## 💡 Оптимизация

### Уменьшить частоту polling

В `client/js/NetworkHTTP.js`:
```javascript
this.pollingInterval = 300;  // вместо 100 (меньше запросов)
```

### Предотвратить "засыпание"

Используйте uptime-сервис:
- https://cron-job.org
- https://uptimerobot.com

Отправляйте GET запрос каждые 10 мин:
```
https://<your-app>.onrender.com/api/rooms
```

---

## 📋 Чеклист развёртывания

- [ ] `server/http-pure.js` существует
- [ ] `client/index-http.html` существует
- [ ] `client/js/NetworkHTTP.js` существует
- [ ] Start Command: `node server/http-pure.js`
- [ ] NODE_VERSION=22.22.0
- [ ] PORT=3000
- [ ] Деплой успешен
- [ ] URL открывается

---

## 🎮 Тестирование

1. Откройте 2 вкладки браузера
2. `https://<your-app>.onrender.com/`
3. Введите разные имена
4. Создайте комнату в первой вкладке
5. Присоединитесь во второй
6. Нажмите "Start Game"
7. Играйте! (WASD + мышь)

---

## 📞 Ссылки

- **Render Dashboard**: https://dashboard.render.com
- **Документация**: https://render.com/docs
- **Статус**: https://status.render.com

---

## 🎉 Готово!

Игра работает на **Render Free** через Pure HTTP Long Polling!

**Никаких "Upgrade Required" ошибок!** ✅
