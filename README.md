# 🎮 Pudge Wars - Multiplayer Hook Battle Game

Браузерная многопользовательская игра в реальном времени, вдохновлённая механикой "Pudge Wars" из Dota 2.

## 🚀 Быстрый старт

### Локально

```bash
npm install
npm start
# Откройте client/index.html в браузере
```

### Развёртывание (Бесплатно)

**⚠️ Render Free НЕ работает** (блокирует WebSocket)

**✅ Используйте Koyeb (без карты):**

1. https://app.koyeb.com/auth/signup
2. New App → Deploy from GitHub
3. Build: `npm install`, Start: `npm start`, Port: `3000`
4. Deploy!

**✅ Или Northflank (2 сервиса):**

1. https://northflank.com
2. New Project → Import from Git
3. Build: `npm install`, Start: `npm start`

📖 Подробнее: [PLATFORMS.md](PLATFORMS.md)

## 📋 Особенности

- **Реальное время**: WebSocket соединение для мгновенной синхронизации
- **Серверная авторитетность**: Вся игровая логика на сервере для защиты от читов
- **Система комнат**: Создание и присоединение к игровым комнатам
- **Хук-механика**: Стрельба крюком, попадание и притягивание врагов
- **Физика**: Движение, коллизии, притягивание
- **Боевая система**: Здоровье, смерть, возрождение
- **Анти-чит**: Валидация позиций и скорости игроков

## 🏗️ Архитектура проекта

```
Pudge_Wars/
├── package.json              # Зависимости Node.js
├── server/
│   ├── config.js             # Конфигурация сервера
│   ├── src/
│   │   └── index.js          # Точка входа сервера
│   ├── models/
│   │   ├── Player.js         # Класс игрока
│   │   └── Room.js           # Класс комнаты
│   ├── managers/
│   │   ├── PlayerManager.js  # Управление игроками
│   │   └── RoomManager.js    # Управление комнатами
│   ├── handlers/
│   │   └── MessageHandler.js # Обработка WebSocket сообщений
│   └── physics/
│       └── PhysicsEngine.js  # Физический движок
└── client/
    ├── index.html            # HTML страница
    ├── css/
    │   └── style.css         # Стили интерфейса
    └── js/
        ├── Network.js        # WebSocket клиент
        ├── Input.js          # Обработка ввода
        ├── Renderer.js       # Отрисовка на Canvas
        ├── Game.js           # Игровая логика
        ├── UI.js             # Управление интерфейсом
        └── main.js           # Точка входа клиента
```

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
npm install
```

### 2. Запуск сервера

```bash
npm start
```

Или в режиме разработки с авто-перезагрузкой:

```bash
npm run dev
```

### 3. Открытие клиента

Откройте `client/index.html` в браузере или используйте локальный сервер:

```bash
# Python
python -m http.server 8080 --directory client

# Node.js (требуется http-server)
npx http-server client -p 8080
```

Перейдите по адресу `http://localhost:8080`

## 🎮 Управление

| Клавиша | Действие |
|---------|----------|
| W / ↑   | Движение вверх |
| S / ↓   | Движение вниз |
| A / ←   | Движение влево |
| D / →   | Движение вправо |
| ЛКМ     | Выстрел хуком |

## 📡 Протокол WebSocket

### Клиент → Сервер

| Тип | Описание | Данные |
|-----|----------|--------|
| `register` | Регистрация игрока | `{ name: string }` |
| `createRoom` | Создание комнаты | `{ roomName, options }` |
| `joinRoom` | Присоединение к комнате | `{ roomId }` |
| `leaveRoom` | Выход из комнаты | - |
| `getRoomList` | Запрос списка комнат | - |
| `startCountdown` | Запуск отсчёта | - |
| `cancelCountdown` | Отмена отсчёта | - |
| `input` | Ввод игрока | `{ input, sequence }` |
| `chatMessage` | Сообщение чата | `{ message }` |

### Сервер → Клиент

| Тип | Описание | Данные |
|-----|----------|--------|
| `registered` | Подтверждение регистрации | `{ playerId, name }` |
| `roomCreated` | Комната создана | `{ room, player }` |
| `roomJoined` | Присоединение к комнате | `{ room, player }` |
| `roomLeft` | Выход из комнаты | - |
| `roomList` | Список комнат | `{ rooms: [] }` |
| `playerJoined` | Игрок присоединился | `{ player }` |
| `playerLeft` | Игрок вышел | `{ playerId }` |
| `countdownStart` | Начало отсчёта | `{ duration }` |
| `countdownUpdate` | Обновление отсчёта | `{ remaining }` |
| `countdownCancelled` | Отмена отсчёта | - |
| `gameStart` | Начало игры | `{ players, arena }` |
| `gameEnd` | Конец игры | `{ winner, leaderboard }` |
| `gameState` | Состояние игры | `{ players, timestamp }` |
| `hookActivated` | Хук активирован | `{ playerId, angle, x, y }` |
| `hookHit` | Попадание хуком | `{ shooter, target, x, y }` |
| `hookMiss` | Промах хуком | `{ playerId, x, y }` |
| `playerKilled` | Убийство | `{ killer, victim }` |
| `death` | Смерть игрока | `{ killerId, respawnIn }` |
| `respawn` | Возрождение | `{ x, y, hp }` |
| `error` | Ошибка | `{ error, details }` |

## ⚙️ Конфигурация

### server/config.js

```javascript
{
  PORT: 3000,                    // Порт сервера
  GAME: {
    TICK_RATE: 60,               // Обновлений в секунду
    ARENA_WIDTH: 1200,           // Ширина арены
    ARENA_HEIGHT: 800,           // Высота арены
    PLAYER_RADIUS: 20,           // Радиус игрока
    PLAYER_SPEED: 200,           // Скорость игрока
    PLAYER_MAX_HP: 100,          // Максимальное здоровье
    RESPAWN_TIME: 3000,          // Время возрождения (мс)
  },
  HOOK: {
    SPEED: 600,                  // Скорость хука
    RANGE: 500,                  // Дальность хука
    RADIUS: 8,                   // Радиус хука
    COOLDOWN: 2000,              // Перезарядка (мс)
    PULL_SPEED: 300,             // Скорость притягивания
    DAMAGE: 25,                  // Урон от хука
  },
  ROOM: {
    MAX_PLAYERS: 10,             // Максимум игроков
    MIN_PLAYERS: 2,              // Минимум для старта
  },
}
```

## 🔒 Анти-чит

Сервер реализует следующие проверки:

1. **Валидация скорости**: Проверка максимально возможного перемещения
2. **Проверка границ**: Контроль выхода за пределы арены
3. **Последовательность ввода**: Отслеживание последовательных номеров ввода
4. **Серверная авторитетность**: Клиент только отправляет ввод, не состояния

## 🎯 Расширение

### Добавление новых способностей

1. Создайте класс способности в `server/abilities/`
2. Добавьте обработку в `Player.js`
3. Реализуйте клиентскую отрисовку в `Renderer.js`

### Добавление новых режимов игры

1. Создайте новый класс режима в `server/modes/`
2. Интегрируйте в `Room.js`
3. Добавьте UI элементы

## 📝 Лицензия

MIT License

## 🙏 Благодарности

Вдохновлено кастомной игрой "Pudge Wars" для Dota 2.
