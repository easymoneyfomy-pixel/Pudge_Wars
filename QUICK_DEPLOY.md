# 🚀 Быстрое развёртывание Pudge Wars

## ⚡ Самый быстрый способ (5 минут)

### Railway (Рекомендуется)

```bash
# 1. Клонирование
git clone <your-repo-url>
cd Pudge_Wars

# 2. Установка Railway CLI
npm install -g @railway/cli

# 3. Авторизация
railway login

# 4. Инициализация и деплой
railway init
railway up
```

**После деплоя:**
- Railway выдаст URL вида: `https://pudge-wars-production.up.railway.app`
- Откройте `https://pudge-wars-production.up.railway.app/client/` в браузере

---

## 🎯 Пошаговая инструкция

### Шаг 1: Подготовка

```bash
# Проверка Node.js
node --version  # Должна быть версия 18+

# Установка зависимостей
npm install
```

### Шаг 2: Выбор платформы

#### Railway (Просто)
1. Перейдите на https://railway.app
2. Sign up через GitHub
3. New Project → Deploy from GitHub repo
4. Выберите ваш репозиторий
5. Готово!

#### Fly.io (Продвинуто)
```bash
# Установка
curl -L https://fly.io/install.sh | sh

# Авторизация
fly auth login

# Создание приложения
fly launch --name pudge-wars --org personal --region ord

# Деплой
fly deploy

# Открыть
fly open
```

#### Oracle Cloud (Production)
1. https://cloud.oracle.com → Sign Up
2. Create VM Instance (Ampere A1)
3. SSH: `ssh -i key.pem ubuntu@<ip>`
4. Установка:
```bash
curl -fsSL https://deb.nodesource.com/setup_22.x | sudo -E bash -
sudo apt install -y nodejs git

git clone <repo>
cd Pudge_Wars
npm install

# Запуск через PM2
sudo npm install -g pm2
pm2 start server/src/index.js --name pudge-wars
pm2 startup
pm2 save
```

### Шаг 3: Настройка клиента

После деплоя сервера:

1. Откройте браузер
2. Перейдите на: `https://<your-server-url>/client/`
3. Введите имя и подключитесь

---

## 🔧 Конфигурация

### Переменные окружения

| Платформа | PORT | NODE_VERSION |
|-----------|------|--------------|
| Railway | Авто | 22.22.0 |
| Fly.io | 3000 | 22 |
| Oracle | 3000 | 22 |

### Изменение настроек игры

Отредактируйте `server/config.js`:

```javascript
module.exports = {
  PORT: process.env.PORT || 3000,
  GAME: {
    TICK_RATE: 60,
    ARENA_WIDTH: 1200,
    // ...
  }
}
```

---

## 🐛 Troubleshooting

### "Upgrade Required" на Render
Render требует платный тариф для WebSocket. Используйте Railway или Fly.io.

### Клиент не подключается
1. Проверьте URL (должен быть `wss://` для HTTPS)
2. Откройте консоль браузера (F12) для ошибок
3. Убедитесь, что сервер запущен

### "Cannot find module"
Все импорты используют `path.join(__dirname, ...)` для кроссплатформенности.

### Высокий пинг
1. Выберите регион ближе к игрокам
2. Уменьшите TICK_RATE в config.js

---

## 📊 Мониторинг

### Railway
```bash
railway logs
railway status
```

### Fly.io
```bash
fly logs
fly status
fly apps info
```

### Oracle/PM2
```bash
pm2 logs
pm2 status
pm2 monit
```

---

## 💰 Стоимость

| Платформа | Free Tier | Paid от |
|-----------|-----------|---------|
| Railway | $5 кредитов | $5/мес |
| Fly.io | Да (ограничено) | $1.94/мес |
| Oracle | Да (навсегда) | Pay-as-you-go |
| Render | Нет | $7/мес |

---

## 🎮 Тестирование

1. Откройте 2+ вкладок браузера
2. Подключитесь с разными именами
3. Создайте комнату в одной вкладке
4. Присоединитесь в другой
5. Нажмите "Start Game"
6. Играйте! (WASD + мышь для хука)
